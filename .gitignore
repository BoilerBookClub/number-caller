# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase CLI deploy cache
.firebase/

# Secrets. .env.example files are the documented, checked-in templates.
.env
.env.*
!.env.example
!functions/.env.example

# Local emulator state
.emulator-data/
firebase-export-*/

# Local scratch: one-off harnesses and probe scripts for looking at a component
# on its own. Also ignored by eslint — see eslint.config.js.
harness.*
scratch-*
